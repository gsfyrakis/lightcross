Lightcross: Vulnerability scanning Tool for smart contracts

Installation
- install dependencies
      pip install mythril
      pip install slither-analyzer
- execute python smartvulscan.py <smart contract folder>


Specific versions of eth-hash and protobuf could be needed
pip install protobuf==4.21.6
pip install eth-hash==0.51

